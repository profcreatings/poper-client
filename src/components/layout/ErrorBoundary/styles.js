import styled from '@emotion/styled';

export const Wrapper = styled.div`
    height: calc(var(--100vh, 100vh) - 2 * var(--body-padding));

    display: flex;
    align-items: center;
    justify-content: center;
`;
